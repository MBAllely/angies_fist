import { dojoPopularity } from 'angies-fist/helpers/dojo-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | dojo popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dojoPopularity([42]);
  assert.ok(result);
});
